---
interface Props {
  /**
   * タイトル
   */
  title: string;
}
const { title } = Astro.props;
---

<div class="flex flex-col md:flex-row md:gap-4 my-1 md:my-2">
  <dt class="font-bold w-16 text-black dark:text-white">{title}</dt>
  {
    /* **********
  TIPS: 子要素は<slot />で書ける
  ***************/
  }
  <dd class="flex-1 text-black dark:text-white mt-1 md:mt-0 overflow-hidden"><slot /></dd>
</div>
